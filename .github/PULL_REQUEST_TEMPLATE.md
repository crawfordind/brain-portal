## What changed

<!-- One or two sentences. What does this do that the code did not do before? -->

## Why

<!-- The problem, the bug report, or the issue this closes.
     If you tried a simpler approach that did not work, that is worth a line —
     it stops the next person retrying it. -->

Closes #

## How to check it

<!-- What a reviewer should do to see this working. A command, a page to open,
     a test that now passes. -->

## Checklist

- [ ] `npm run lint` — no new errors
- [ ] `npm run typecheck` — clean
- [ ] `npm test` — passing
- [ ] Tests added or updated, if this fixes a bug or adds behaviour
- [ ] Docs updated, if this changes setup, configuration or an API
- [ ] No secrets, personal data or real names in the diff

<!-- If this touches auth, permissions, uploads, or anything that fetches a
     user-supplied URL, say so here so it gets a closer read. -->
